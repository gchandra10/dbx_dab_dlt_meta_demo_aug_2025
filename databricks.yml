# This is a Databricks asset bundle definition for dbx_dab_dlt_meta_demo_aug_2025.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dbx_dab_dlt_meta_demo_aug_2025
  uuid: fb711a7b-ceb7-4054-a49c-cf1d53702692

include:
  - resources/*.yml
sync:
  exclude:
    - .gitignore
    - .DS_Store
    - .vscode
    - bundle_config_schema.json
    - .venv/

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-984752964297111.11.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/ganesh.chandra@databricks.com to make sure we only have a single copy.
      root_path: /Workspace/Users/ganesh.chandra@databricks.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: ganesh.chandra@databricks.com
        level: CAN_MANAGE
